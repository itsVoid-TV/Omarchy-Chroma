# Omarchy Chroma user configuration
# This file is sourced as Bash. Changes apply in newly opened terminals.

# Examples:
# CHROMA_STYLES[install]='fg=yellow,bold'
# CHROMA_STYLES[danger]='fg=white,bg=red,bold'

# Add your own executable names to a semantic group:
# CHROMA_EXTRA_INSTALL_COMMANDS+=(my-installer)
# CHROMA_EXTRA_REMOVE_COMMANDS+=(my-uninstaller)
# CHROMA_EXTRA_DANGER_COMMANDS+=(my-disk-wiper)

# Automatic ghost-text suggestions are disabled by default. Set this to 1 if
# you want ble.sh's suggestions while typing. Tab completion is unaffected.
# CHROMA_SUGGESTIONS=1

# Omarchy's active theme is followed automatically. Chroma preserves each
# theme's hue but raises faint colors to this minimum contrast ratio.
# CHROMA_THEME_INTEGRATION=1
# CHROMA_MIN_CONTRAST=5.5

# Set to 0 only if you intentionally manage fzf/ble.sh integration yourself.
# CHROMA_FZF_INTEGRATION=0
